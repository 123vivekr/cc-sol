#include <stdio.h>

int main()
{
    int n, f = 1;
    scanf("%d", &n);
    for(int i = 0; i < n && (1 + 0 * (i != 0 && printf("that "))); ++i ) {
        if(f == 1)
            printf("I hate ");
        else
            printf("I love ");
        f *= -1;
    }
    printf("it\n");
    return 0;
}
